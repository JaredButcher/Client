"""Client application unit testing"""
import os
import sys
#sys.path.insert(0, os.path.dirname(sys.path[0]) + os.sep + 'src' + os.sep + 'internal')
#sys.path.insert(0, os.path.dirname(sys.path[1]) + os.sep + 'src')

from meg_runtime import Config, Plugin, PluginManager

def test_client():
    """TODO: Test client"""
    pass
